#ifndef HUD_H
#define HUD_H

#include "../adt/struct.h"

void insertNewSeg(Root);
void dashboard(Root);
void showSeg(Root);
void salvataggio(Root);
void removeSeg(Root);
void init_search_seg(Root);


#endif
